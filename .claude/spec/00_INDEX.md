# spec/ — SoT 지식 인덱스 (읽기전용)

> 이 폴더는 **진실의 원천(Source of Truth)**. 사실은 오직 여기서만 Read 한다. 수정·생성 금지(PreToolUse 훅이 차단).
> 본문 곳곳에서 문서를 **번호 약어**로 인용한다 — 예: `03 §6`, `04 §12`, `01 §6-5`. 아래 표가 그 약어의 매핑이다.

## 문서 목록 & 관할

| 약어 | 파일 | 이 문서가 정하는 것 |
|---|---|---|
| **01** | `01_uss_admin_wireframe_spec.md` | 화면 동작·IA·상태전이·검증 규칙·모달·빈/로딩/에러 상태 |
| *(02)* | *(없음)* | 백엔드 명세 몫이라 이 프론트 하네스 범위 밖 — 번호가 빈 건 **누락이 아니다** |
| **03** | `03_uss_admin_api_spec.md` | API 계약 — 엔드포인트·필드·상태코드·에러 포맷·enum·토큰 정책·도메인 사전 |
| **04** | `04_uss_admin_frontend_spec.md` | 프론트 아키텍처·스택·Step 절차·횡단정책·MSW + §2 문서 정합성 노트 |
| **DS-00** | `DS-00_uss_overview.md` | **시각 방향** — 톤·레퍼런스·디자인 원칙·문장 어조·채택/미채택 |
| **DS-01** | `DS-01_uss_design_system.md` | **디자인 토큰 값·컴포넌트 명명·상태 변형·아이콘** |
| **DS-03** | `DS-03_interactions.md` | 상호작용 패턴(모달·토스트·검증·4상태). ⚠️ **Gravit 원문 승계** — 절별 적용 가부는 아래 표 |

#### DS-03 절별 적용 범위 (Gravit 원문이라 그대로 쓰면 안 된다)

> **읽을 곳만 읽는다.** 442행 전체를 통독하면 70%가 USS 와 무관한 내용이다.
> `Read` 의 `offset`/`limit` 으로 ✅ 구간만 가져온다.

| 절 | 행 | USS 적용 |
|---|---|---|
| §1-1·§1-2 모달 기본·variant 매핑 | **18-55** | ✅ 사용 |
| §1-3 Strict Match Modal | 56-95 | ❌ 스테이징 promote 전용 — USS 와 무관하다. M4 경고는 **`01`** 을 따른다 |
| §2-1 토스트 variant | **96-110** | ✅ 사용 |
| §2-2 토스트 표준 문구 | 111-126 | ❌ 격식체인 데다 Gravit 도메인(공지·스테이징·신고)이다. USS 문구는 **`ui-conventions`** 를 따른다(짧은 구어체 15~25자, M4 만 격식체) |
| §3-1·§3-2 검증 시각·트리거 | **127-151** | ✅ 사용 |
| §3-3 화면별 검증 규칙 | 152-200 | ❌ Gravit 16화면 — USS 검증은 **`01`** |
| §4 Empty/Loading/Error | **201-238** | ✅ 사용 |
| §5 화면별 상호작용 | 239-412 | ❌ 전부 Gravit 16화면(§5-12·§5-13 스테이징 포함) — USS 는 **`01 §4~§7`** |
| §6-1 라우팅/네비 | 413-421 | ⚠️ 사이드바 전제라 부분 무관. USS 는 사이드바 없음 |
| §6-2 페이지네이션 동작 | **422-430** | ✅ 동작만 사용. **행 수는 10**(`decisions`) |
| §6-3 모달 공통 | **431-443** | ✅ 사용 |

> 실제로 읽을 구간은 **18-55 · 96-110 · 127-151 · 201-238 · 422-443**(약 130행). 나머지 310행은 Gravit 전용이다.

### ⚠️ USS 화면은 2개다

`ADMIN_LOGIN`(`/login`) · `SYNC_MAIN`(`/`) + 모달 5종(M1~M5). 세 번째 화면을 만들지 않는다(`decisions` D8).

### ⚠️ Gravit 문서로는 판단하지 않는다

`DS-02_screens.md`(16화면)·구 `DS-00_overview.md`·구 `DS-01_design_system.md` 는 **폐기**했다. 남아 있다면 무시한다. USS 화면의 시각 근거는 **`01 §4~§7`** 이다.

## 충돌 시 우선순위 (위가 이긴다)

1. **명시적 결정** — `.claude/rules/decisions.md`(D1~D12 + 기타 확정값)
2. **해당 영역의 관할 문서**(위 표)
3. 그 외 문서
4. **코드 ↔ spec 충돌** → 항상 spec 우선
5. **Gravit 잔재 ↔ USS 문서** → 무조건 USS 문서

## 멈춤 규칙

- spec 에 없거나 서로 모순이고 `decisions` 로도 해소 안 되면 → 코드 작성 금지, 사용자에게 질문(🙋🏻).
- `03 §11-1` 실측 8항목은 **미검증**이다. 그 결과에 의존하는 값을 하드코딩하지 않는다.
- 우선순위·충돌 해소 상세: `.claude/rules/source-of-truth.md`
