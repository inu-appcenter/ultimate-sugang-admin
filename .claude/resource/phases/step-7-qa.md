# Step 7 — QA & 통합 검증

## 읽을 것 (이 목록이 전부다 — 규약은 `phase-0-recovery.md` §읽기 규약)

| 절 | 여기서 얻는 것 |
|---|---|
| `04 §12` | Step 7 시나리오 14개 |
| `04 §11-3` | MSW 시나리오 12개 |

> 행은 `node .claude/hooks/checks/spec-map.mjs "03 §6"` 이 준다. 목록 밖의 동작·데이터·계약이 필요해지면 멈추고 묻는다(🙋🏻).

## 절차
1. 게이트 `--with-smoke` 실행.
2. `04 §12 Step 7` 의 **시나리오 14개** 를 순서대로 검증하고 결과를 보고.
3. **DS 일관성 점검** — 토큰만 사용 / 제거 토큰 미참조 / 카드 보더 없음 / radius 14·10·16 / `text-metric` 적용 / Primary 5~10% / Badge variant / 모달 400·480 / 4상태 / **문구 어조(M4 격식체)**.
4. 리뷰어 3종(spec·architecture·ds) 실행 → 전부 PASS 필요.
5. 미해결 이슈 목록화.

## 출력
시나리오 14개 결과표 + DS 일관성 보고서 + 미해결 목록.

## 다음
없음. 사용자 최종 승인.
